upstream appraisal_backend {
    server 127.0.0.1:8000;
}

# This configuration will be replaced by setup_ssl.sh when SSL is available
# For now, nginx will only serve on port 443 if SSL certificates exist 